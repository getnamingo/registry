<?php

// Add operator-specific EPP settings here. Docker-managed database, listener,
// and certificate paths are applied after these values.
return [
    // 'ns_mode' => 'hostObj',
    // 'ssl_client_ca' => '/certs/registrar-ca.pem',
];
